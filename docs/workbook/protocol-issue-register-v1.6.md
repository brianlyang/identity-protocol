# Identity Protocol v1.6 Issue Register

Status: Canonical current-status register
Layer: protocol
Scope: cross-stream issue governance for the `v1.6` workbook family, radiating over the active `v1.6.x` stream lanes
Authority: this file is the authoritative current-status table for the active workbook family selected by `identity/protocol/mappings/workbook-registry.current.yaml`.

## 0) Fixed role boundary

1. This file is not a stream review ledger and does not replace `docs/review/` owner ledgers.
2. This file is not a semantic SSOT and cannot redefine stream semantics already frozen in owner governance docs.
3. This file is the canonical protocol-side current-status register for cross-stream issue cleanup across the `v1.6` minor family.
4. `docs/workbook/protocol-deep-audit-workbook-v1.6.md` is the companion routing/intake workbook, but current status authority stays here.
5. No external workspace workbook is authoritative for this lane; any mirror outside `identity-protocol-local` is projection-only and may be deleted without changing protocol truth.
6. Optional workspace projections, if kept, must be declared through `identity/protocol/mappings/workbook-registry.v1.6.yaml` and may never override this table.

## 1) Current control-plane alias refs

- `identity/protocol/mappings/workbook-registry.current.yaml`
- `identity/protocol/mappings/stream-doc-registry.current.yaml`
- `identity/protocol/mappings/control-plane-status.current.yaml`

## 2) Current machine recheck lock

- `scripts/validate_issue_register_consistency.py --json-only` -> `PASS_REQUIRED`
- `scripts/docs_command_contract_check.py` -> `PASS` (`docs checked: 102`, `command snippets checked: 1382`)
- `scripts/validate_native_chat_bootstrap_entry_stream.py --json-only` -> `PASS_REQUIRED` with `standard_closure_status=CLOSED`, `promotion_status=PROMOTION_REVIEW_ELIGIBLE`, and `no_silent_headerless_turn_status=PASS_REQUIRED`

## 3) Issue table

| Issue | Status | Primary owner lane | Current closure anchor | Freeze rule |
| --- | --- | --- | --- | --- |
| ISSUE-001 stream scope matrix incompleteness | CLOSED | `v1.6.8` | `identity/protocol/mappings/stream-scope-matrix.v1.6.yaml` now contains active rows for `v1.6.11` / `v1.6.12` / `v1.6.13`; `scripts/validate_stream_scope_semantic_integrity.py` now emits `missing_scope_rows=[]` and `archived_doc_collisions=[]`. | Keep the stream-scope validator as the anti-regression gate. |
| ISSUE-002 strict authority consumer touches compatibility pointer | CLOSED | `v1.6.10` | `scripts/render_identity_response_stamp.py` fail-closes instead of reading `session/active_identity.json`; `scripts/validate_response_authority_consumer_semantics.py` returns `PASS_REQUIRED`. | Keep strict consumers pointer-free; only replay/status tooling may read compatibility mirrors. |
| ISSUE-003 required-gate drift validator launch/parser fragility | CLOSED | `v1.6.10` | `scripts/validate_required_gate_surface_drift.py` now self-resolves repo root, recognizes `python3` delegation inside wrapper functions, ignores heredoc `python3 -`, and normalizes variable-based script targets. | Keep drift checks bound to machine extraction, not cwd-dependent shell literals. |
| ISSUE-004 historical baseline docs still behave as live defaults | CLOSED | `v1.6.0` motherline | `scripts/validate_historical_baseline_default_boundary.py` now proves live default surfaces resolve governance/review docs via `contract-binding.current.yaml`, while historical motherline literals remain only in explicit checker surfaces. | Keep historical docs as traceability anchors only; block any new direct default literal. |
| ISSUE-005 old-version-named active carriers remain current pointers | CLOSED | `v1.6.0` motherline | versioned `*.current.yaml` carriers now carry explicit `pointer_contract=frozen_versioned_active_carrier`, `upgrade_switch_mode=pointer_only`, and `replay_snapshot_immutable=true`; `scripts/validate_current_alias_versioned_carrier.py` enforces the freeze. | Upgrade future carriers by switching the pointer intentionally, not by silent filename drift. |
| ISSUE-006 temp/probe shared infra is not mandatory yet | CLOSED | `v1.6.10` | `scripts/runtime_temp_path_common.py` / `.sh` now back the remaining live temp callsites (`create_identity_pack.py`, `validate_identity_switch_closure_semantics.py`, `run_native_chat_bootstrap_entry_probes_ci.sh`); `scripts/validate_runtime_temp_path_contract.py` guards the convergence. | Reuse the shared helper for any new temp-producing runtime/probe surface. |
| ISSUE-007 Batch-6/7 compatibility wrapper residue remains active | CLOSED | `v1.6.0` motherline | `scripts/validate_active_validator_alias_residue.py` now scans protocol strict targets plus active runtime-pack `CURRENT_TASK.json` surfaces discovered from `.identity/catalog.local.yaml`; the reopened local packs were backfilled to `scripts/validate_intake_evidence_core.py`, and `rg -n '"validator": "scripts/validate_v16_' .identity/*/CURRENT_TASK.json` now returns no hits. | Keep the alias-residue gate bound to contract-binding + local runtime-pack inventory so wrapper ids cannot silently return. |
| ISSUE-008 archived `v1.6.9` semantic pollution risk | CLOSED | `v1.6.8` | `scripts/validate_stream_scope_semantic_integrity.py` now checks archived-doc collisions; current recheck reports `archived_doc_collisions=[]`. | Keep archived streams in `legacy_archival_docs` only and block active-registry reuse by validator. |
| ISSUE-009 `v1.6.12` promotion-lane parity drift | CLOSED | `v1.6.12` | `scripts/validate_native_chat_bootstrap_entry_stream.py` stays `PASS_REQUIRED`; `bash identity-protocol-local/scripts/ci/run_host_visible_surface_live_probes_ci.sh` and `bash identity-protocol-local/scripts/ci/run_native_chat_bootstrap_entry_probes_ci.sh` pass. | Keep promotion enhancement open without reopening standard closure. |
| ISSUE-010 compatibility pointer still carries canonical-pointer terminology | CLOSED | `v1.6.10` | live sync/repair/status surfaces now emit `compatibility_mirror_pointer_path` / `session_pointer_compatibility_path` instead of canonical-pointer labels, and `scripts/validate_compatibility_pointer_terminology.py` guards the boundary. | Keep canonical-pointer wording out of live compatibility payloads; legacy traces stay historical only. |
| ISSUE-011 validator repo-root resolution cwd coupling | CLOSED | `v1.6.10` | `scripts/repo_root_resolution_common.py` is consumed by repo-root-sensitive validators; both workspace-root and protocol-root launches now return identical PASS verdicts. | Reuse the helper for later validators instead of adding new `default="."` surfaces. |
| ISSUE-012 top-level kernel overview / release metadata drift against the active draft head | CLOSED | `v1.6.0` motherline | `python3 identity-protocol-local/scripts/validate_release_metadata_sync.py --json-only` now returns `PASS_REQUIRED`, with `identity/protocol/IDENTITY_PROTOCOL.md`, `README.md`, `VERSIONING.md`, and `requirements-dev.txt` all aligned on `v1.6.14`. | Keep every top-level release marker in lock-step with the active draft head; any future draft-head advance must land with the validator still green. |
| ISSUE-013 stream-doc command and current-alias contract drift | CLOSED | `v1.6.8` | `scripts/docs_command_contract_check.py` now self-resolves repo root, validates workspace-owned script references, and returns `PASS` from both workspace root and protocol root; repaired docs now include the required current-alias refs, and `identity/protocol/IDENTITY_PROMPT_BOOTSTRAP_CONTRACT.md` now points at `scripts/validate_prompt_kernel_executable_coupling.py`. | Keep stream-doc command checks repo-root invariant and let the checker, not manual triage, be the release gate for future doc drift. |
| ISSUE-014 legacy compatibility taxonomy has crossed into an active machine-gate default | CLOSED | `v1.6.10` + compiled-brief lane | `scripts/compile_identity_runtime.py`, `scripts/validate_compiled_brief_projection_boundary.py`, and regenerated `identity/runtime/IDENTITY_COMPILED.md` now use `tracked_compiled_brief_frozen_path`; `docs/governance/identity-runtime-file-governance-control-plane-v1.6.10.md`, `docs/review/protocol-remediation-audit-ledger-v1.6.10-runtime-file-governance.md`, and `identity/protocol/mappings/semantic-term-registry.v1.6.yaml` explicitly narrow `legacy_canonical_compatibility_path` to governance/migration surfaces only. | Keep `tracked_compiled_brief_frozen_path` as the only pass-default compiled-brief path token; any return of the legacy term to compiled-brief outputs or positive gates reopens the issue. |
| ISSUE-015 Batch-6/7 active docs still normalize compatibility wrappers as canonical executables | CLOSED | `v1.6.0` motherline | `docs/governance/identity-actor-session-binding-governance-v1.6.0.md` and `docs/review/protocol-remediation-audit-ledger-v1.6.md` now use canonical non-versioned executables; `identity/protocol/mappings/contract-binding.v1.6.yaml` records versioned aliases as `wrapper_compatibility_optional`, and `scripts/validate_contract_binding_reference_integrity.py` now fail-closes when docs use a versioned wrapper as a canonical executable without explicit wrapper/compatibility context. | Keep docs, mapping, and checker aligned on canonical-vs-wrapper roles; any active doc line that uses a versioned wrapper as canonical executable must fail-close. |
| ISSUE-016 required-gate bundle parity missed `ASB16-RQ-042` / `ASB16-RQ-043` | CLOSED | `v1.6.8` | `scripts/required_gate_bundle_runner.py` now absorbs `asb16-rq-042` / `asb16-rq-043`, `scripts/validate_control_plane_invariants.py` returns `PASS_REQUIRED`, and machine-maintained refreshes through `scripts/render_control_plane_budget.py --write` plus `scripts/render_control_plane_status.py --write` restore budget/status parity to green. | Keep bundle-row growth governed by mapping + machine-maintained budget/status refresh instead of hand-editing envelopes after new motherline rows land. |
| ISSUE-017 operational repo-root invariance remains incomplete outside validator lane | CLOSED | `v1.6.10` | `scripts/repo_root_resolution_common.py` now exposes protocol/workspace root self-resolution, `scripts/render_control_plane_status.py` and `scripts/sync_plugin_join_wiring.py` auto-resolve the protocol repo root, and `scripts/scan_identity_path_residue.py` auto-resolves the workspace root; workspace-root and protocol-root replays now agree. | Reuse the shared resolver helpers for later operational tools; treat any remaining `default="."` launch surface as a regression. |
| ISSUE-018 `v1.6.14` launcher stream docs-contract drift was detected and repaired in-owner lane | CLOSED | `v1.6.14` | `docs/review/protocol-remediation-audit-ledger-v1.6.14-identity-codex-launcher.md` now includes `identity/protocol/mappings/control-plane-status.current.yaml` and no longer presents future launcher scripts as executable snippets; `scripts/docs_command_contract_check.py` is green from both workspace and protocol roots. | Keep launcher docs strict about current-alias references and never promote future script names to executable status before those files land. |
| ISSUE-019 authoritative identity resolver default local-catalog still follows ambient parent git root | CLOSED | `v1.6.10` | `python3 identity-protocol-local/scripts/resolve_identity_context.py resolve --identity-id base-repo-architect` now returns `source_layer=project` from the workspace root by default, and `python3 identity-protocol-local/scripts/validate_resolve_identity_context_default_local_catalog.py --json-only` returns `PASS_REQUIRED` for both workspace-root and protocol-root CLI replays. | Keep authoritative default resolution self-rooted to the project runtime and keep the direct-CLI validator green. |
| ISSUE-020 active scope/state/repair scripts still default directly to global `~/.codex/.identity/catalog.local.yaml` | CLOSED | `v1.6.10` | `validate_identity_scope_resolution.py`, `validate_identity_scope_persistence.py`, `validate_identity_state_consistency.py`, `repair_identity_baseline_evidence.py`, `repair_identity_feedback_evidence.py`, `repair_identity_arbitration_evidence.py`, `repair_identity_install_evidence.py`, `repair_identity_replay_evidence.py`, and `sync_identity_meta_status.py` now default through `default_local_catalog_path(...)`, while `python3 identity-protocol-local/scripts/validate_cli_catalog_default_semantics.py --json-only` returns `PASS_REQUIRED` with zero global-home fallback hits. | Keep active scope/state/repair defaults routed through shared runtime-path semantics; any new direct global-home catalog fallback must fail in the validator. |
| ISSUE-021 active direct-entry scripts still bind default `--repo-catalog` to caller launch root | CLOSED | `v1.6.10` | `scripts/resolve_identity_context.py` now exposes shared `resolve_repo_catalog_path(...)`, and `validate_fixture_runtime_boundary.py`, `validate_protocol_entry_candidate_bridge.py`, and `render_identity_response_stamp.py` all consume it. Workspace-root replays of the three commands now advance to their semantic verdicts instead of failing on `repo catalog not found`, and `python3 identity-protocol-local/scripts/validate_cli_catalog_default_semantics.py --json-only` now returns `launch_context_parity_status=PASS_REQUIRED` after executing the workspace/protocol parity probes itself. | Keep direct-entry repo-catalog resolution routed through the shared resolver and keep the parity probes inside `validate_cli_catalog_default_semantics.py`; any future caller-cwd coupling on these entry surfaces must fail the machine gate. |
| ISSUE-022 `validate_identity_local_persistence.py` still derives `repo_root = Path.cwd().resolve()` | CLOSED | `v1.6.10` | `validate_identity_local_persistence.py` now resolves the protocol root from the governed repo catalog instead of `Path.cwd()`, and adjacent operational tools (`identity_status.py`, `collect_identity_health_report.py`, `validate_identity_experience_writeback.py`, `validate_identity_scope_resolution.py`, `validate_identity_scope_persistence.py`, `validate_identity_scope_isolation.py`) now self-root default local catalogs to the project runtime. The same absolute-input replay of `validate_identity_local_persistence.py` now passes from both workspace root and protocol root, while `identity_status.py --identity-id base-repo-architect --json` resolves the project runtime pack from both launch contexts. | Keep hidden cwd-root semantics out of persistence/operational tooling by reusing the shared local-catalog and repo-catalog resolvers; any future workspace-root/protocol-root split on the same governed inputs reopens the issue. |
| ISSUE-023 docs command checker still misses launch-context-broken executable snippets | CLOSED | `v1.6.8` | `scripts/docs_command_contract_check.py` now recognizes workspace-root invariant `identity-protocol-local/scripts/...` snippets and runs workspace-root semantic probes for safe path-sensitive executables instead of stopping at script-exists/flag-spelling checks. The cited active docs now use workspace-root invariant commands in `docs/governance/identity-actor-session-binding-governance-v1.6.0.md` and `docs/review/protocol-remediation-audit-ledger-v1.6.md`, and `docs_command_contract_check.py` is green from both workspace root and protocol root. | Keep executable docs workspace-root invariant on this lane, and keep the checker bound to workspace semantic probes so literal path regressions fail in governance before they reappear in audit. |
| ISSUE-024 `v1.6.14` launcher convergence runtime authority and cross-workspace pilot proof were not yet protocol-owned machine truth | CLOSED | `v1.6.14` | The launcher lane now machine-gates runtime-path authority through `runtime_paths_status` in both `scripts/validate_identity_codex_launcher.py` and `scripts/check_identity_codex_launcher_migration_closure.py`; required gates, readiness, and `identity_creator` all consume launcher closure in `workspace-runtime-only` mode; `scripts/ci/run_identity_codex_launcher_cross_workspace_pilot_probes_ci.sh` proves the same convergence entry against another workspace-local runtime catalog with no workspace-specific exception; and launcher convergence receipts now self-pin `evidence_ref` / `manifest_ref` with governed `EVIDENCE_MANIFEST.<run_token>.json` archival under the canonical evidence root. | Keep closer follow-on work confined to broader evidence breadth / archival coverage; any future launcher rollout surface must preserve workspace-runtime-only authority, keep receipt/manifest truth-sync machine-owned, and reuse the same convergence entry rather than introducing workspace-specific wrappers. |
| ISSUE-025 authoritative resolver and active health entrypoints now freeze explicit runtime-catalog authority; remaining raw metadata hygiene stays separate | CLOSED | `v1.6.10` | `scripts/resolve_identity_context.py`, strict-entry consumers, and active runtime utilities now honor explicit runtime-catalog authority through `IDENTITY_CATALOG` / workspace-local `.identity/catalog.local.yaml`; `bash scripts/ci/run_semantic_clarity_probes_ci.sh` and `python3 scripts/validate_cli_catalog_default_semantics.py --json-only` are green on the foreign-project precedence lane and launch-context parity. | Keep resolver truth closed: launchers/resolvers/health entrypoints stay explicit-catalog-first and fail-close; any remaining raw catalog self-description cleanup stays on `ISSUE-029`, not on reopened runtime-catalog fallback semantics. |
| ISSUE-026 session-primary authority now fail-closes compatibility drift; remaining compatibility work stays under quarantine, not live pointer semantics | CLOSED | `v1.6.10` | Strict pointer/health/repair lanes now resolve from session-primary authority first: `scripts/validate_identity_switch_closure_semantics.py --json-only` is green with `compatibility_pointer_identity_authority=diagnostic_only`, `refresh_identity_session_status.py` promotes stale/missing shared projection to `FAIL`, `collect_identity_health_report.py` runs `--strict-session-primary`, `repair_actor_session_authority_residue.py` rebuilds pointer surfaces from authoritative binding, and `validate_identity_session_pointer_consistency.py` no longer exposes the old drift-allow CLI residue. | Keep compatibility projection diagnostic-only and quarantined; any remaining compatibility residue belongs to `ISSUE-027` cleanup/truth-sync, not to warn-level active-runtime pointer behavior. |
| ISSUE-027 motherline no-downgrade contract is now frozen and live residue is quarantined off active payloads | CLOSED | `v1.6.0` motherline | `identity/protocol/IDENTITY_PROTOCOL.md` freezes `rq_047_protocol_no_downgrade_motherline_contract_v1`, `identity/protocol/mappings/contract-binding.v1.6.yaml` binds it as `ASB16-RQ-047`, `scripts/validate_compatibility_legacy_boundary.py` now also machine-checks canonical-only active error payload projection plus replay-only alias echo policy, `scripts/headstamp_error_family_common.py` defaults active surfaces to canonical-only `error_code`, and `docs/governance/identity-headstamp-egress-governance-v1.6.1.md` truth-syncs the replay-only alias rule. | Keep `rq_047` frozen as the lowest-layer rule: compatibility/fallback/bridge stay quarantined to migration/replay/diagnostics, active payloads remain canonical-only, and any attempt to reintroduce replay alias projection on active surfaces reopens the issue. |
| ISSUE-028 declared route/script lane governance is now protocol-owned; remaining browser-manual gaps are instance adoption only | CLOSED | `v1.6.15` | The shared `v1.6.15` owner lane now freezes additive `direct_tool_entry_policy`, canonical `governed_direct_tool_entry` source handling, pre-tool receipt fields (`tool_entry_admission_timing`, `auth_preflight_status`, `session_freshness_status`), and the canonical execution-lane taxonomy (`governed_webhook` -> `webhook_single_flight` + `analysis_webhook`; `governed_direct_tool_entry` -> `tool_admission_serialized` + `interactive_session`) inside the shared validator/probe family. The old `office-ops-expert` browser-manual rescue gap therefore no longer represents missing protocol semantics; it is instance-owned rollout/adoption debt. | Direct MCP/browser execution for a declared route must either enter through route/script/lane admission or fail-close before tools fire, and any live browser/manual success path must declare the canonical lane taxonomy plus admission receipts instead of surviving as undeclared fallback. Remaining pack-specific business executor rollout stays below protocol and must not be misreported as reopened owner-lane debt. |
| ISSUE-029 workspace-local runtime catalog metadata hygiene is now protocol-owned and closed without reopening launcher semantics | CLOSED | `v1.6.10` | `scripts/validate_runtime_catalog_metadata_hygiene.py` and `scripts/repair_runtime_catalog_metadata_hygiene.py` now own raw runtime-catalog self-description as a dedicated hygiene lane; `scripts/check_identity_codex_launcher_migration_closure.py` projects `runtime_catalog_metadata_hygiene_status`; `scripts/run_identity_codex_launcher_workspace_convergence.py` repairs metadata before launcher closure; launcher convergence probes seed `canonical_scope=UNKNOWN` / empty `canonical_pack_path` and prove apply-time repair; required gates and readiness now consume the metadata validator; and `python3 scripts/validate_runtime_catalog_metadata_hygiene.py --catalog ../.identity/catalog.local.yaml --repo-catalog identity/catalog/identities.yaml --require-active --json-only` is green with `checked_identity_count=4`, `violation_count=0`. | Keep raw runtime-catalog metadata hygiene as a separate protocol-owned fail-close lane. Resolver truth stays authority-first, launcher semantics stay closed, and any future raw-row cleanup must extend the same validator/repair family instead of weakening resolver logic or reopening `v1.6.14`. |
| ISSUE-030 routing/learning strengthening symmetry is now landed as a protocol-owned upper-layer runtime contract | CLOSED | `v1.6.17` | `create_identity_pack.py` and `repair_contract_backfill.py` now materialize symmetric `route_discovery_enforcement` / `feedback_operational_prompt_enforcement` hooks under `capability_arbitration_contract`; `scripts/validate_identity_routing_learning_strengthening.py` machine-validates the pair; `scripts/validate_identity_capability_arbitration.py` consumes the new hook family; `required_gate_bundle_runner.py` now binds `ASB16-RQ-048` / `ASB16-RQ-049`; `scripts/ci/run_required_runtime_gates_ci.sh` and `scripts/release_readiness_check.py` now include the strengthening lane; and all four active workspace-local runtime packs replay `PASS_REQUIRED` on the new validator family. | Keep the third/fourth-loop strengthening frozen as generic protocol infrastructure, not business logic. Future pack rollout breadth may continue outside protocol debt, but no backward-compatibility bridge or asymmetric fallback path may re-enter the strengthening lane. |
| ISSUE-031 fourth-loop-to-first-loop loopback bridge is now machine-consumed and closed without semantic collapse | CLOSED | `v1.6.17` | `scripts/validate_feedback_to_judgement_loopback.py` now derives the standalone `feedback_to_judgement_loopback_contract_v1` projection directly from `experience_feedback_contract`, `accurate_judgement_enforcement`, and `feedback_operational_prompt_enforcement`, fail-closing on replay/writeback drift and missing first-loop revalidation prerequisites; `scripts/validate_identity_routing_learning_strengthening.py` now republishes the closed-loop proof as `third_loop_exploration_status`, `fourth_loop_promotion_status`, `first_loop_revalidation_status`, `conflict_demotion_status`, `negative_feedback_writeback_status`, and `live_roundtrip_proof_status`; `scripts/required_gate_bundle_runner.py`, `scripts/release_readiness_check.py`, and `scripts/ci/run_required_runtime_gates_ci.sh` continue consuming `ASB16-RQ-050`; `scripts/ci/run_feedback_to_judgement_loopback_probes_ci.sh` proves positive and negative loopback fixtures plus round-trip projection assertions; and the four active workspace-local runtime identities replay `PASS_REQUIRED` on the loopback lane without ad hoc runtime-pack rewrites. Governance/review/workbook/mapping surfaces remain truth-synced to machine closure rather than a docs-only opening. | Keep the bridge distinct from both the fourth-loop center and the shared four-track primitive. Loopback artifacts remain governed preflight aids only, first-loop revalidation stays authoritative, first-loop conflict must demote or roll back stale prompt artifacts with negative-feedback writeback, and any future expansion of `ASB16-RQ-050` must reuse the same no-downgrade machine consumer lane instead of reopening `ISSUE-030` or inventing a backstop bridge. |
| ISSUE-032 protocol artifact-family routing remains semantically overloaded under generic "memory" wording | CLOSED | `v1.6.18` | `docs/governance/identity-artifact-family-routing-governance-v1.6.18.md` and `docs/review/protocol-remediation-audit-ledger-v1.6.18-artifact-family-routing.md` now freeze the protocol-scoped eight-family routing matrix across pack rulebook, pack task-history, runtime dialogue-retention, runtime dialogue-governance, runtime experience-feedback, runtime protocol-feedback, runtime continuity/reentry, and runtime memory-absorption quarantine families; `rq_051_identity_dialogue_retention_contract_v1` remains the first family-specific machine landing, and `rq_052_identity_artifact_family_routing_contract_v1` now closes the whole-matrix routing lane through `scripts/validate_identity_artifact_family_routing.py`, `scripts/ci/run_identity_artifact_family_routing_probes_ci.sh`, `scripts/create_identity_pack.py`, `scripts/repair_contract_backfill.py`, `scripts/release_readiness_check.py`, `scripts/ci/run_required_runtime_gates_ci.sh`, `scripts/validate_required_contract_coverage.py`, `scripts/required_gate_bundle_runner.py`, and `identity/protocol/mappings/contract-binding.v1.6.yaml` (`ASB16-RQ-052`). Architectural final lock is now frozen for the stream, and current workspace-local replay is `PASS_REQUIRED` for all four active runtime identities on the `rq_052` validator lane; `base-repo-audit-expert-v3` no longer carries protocol-owned required failures on the adopted continuity/artifact-family subset after shared `rq_046` strengthening plus shared `rq_039` dependent-contract backfill, and now also replays `PASS_REQUIRED` on the previously misresolved discovery/tool/vendor lane after `scripts/tool_vendor_governance_common.py` normalized pack-scoped `identity/runtime/**` report contracts onto live runtime-pack roots; and `base-repo-closure-orchestrator` no longer carries the earlier inherited `rq_051` dialogue-retention residual after replaying the protocol-owned post-delivery runtime hook bridge against live runtime evidence. Semantic ownership remains with the `v1.6.18` governance stream, so future breadth/archival truth-sync may continue without reopening routing semantics. | Keep every protocol-owned persisted artifact resolved to one exact family by owner + fixed path + producer + consumer. `memory` is not a canonical sink name; `RULEBOOK.jsonl` must stay distinct from runtime feedback rulebooks, `TASK_HISTORY.md` must stay distinct from continuity, raw dialogue retention must stay distinct from dialogue-governance and continuity, `runtime/protocol-feedback/**` must stay communication-only, and `runtime/memory-absorption/**` must stay quarantine/re-materialization only. Future breadth/archival truth-sync may extend evidence, but it must not redefine family names, canonical roots, producer/consumer roles, or frozen non-goals. |
| ISSUE-033 `v1.6.14` launcher command discovery promoted resume readiness too early and did not freeze fresh-shell-executable resume semantics | CLOSED | `v1.6.14` | `scripts/render_identity_codex_launcher.py` now decomposes command-discovery readiness into `catalog_context_status`, `host_thread_id_status`, `identity_session_tuple_status`, and `resume_command_fresh_shell_executable_status`, and only emits `recommended_resume_command` when resume is fresh-shell executable; `scripts/identity_codex_launcher_common.py` now rejects explicit but non-authoritative `run:<...>` session ids instead of promoting them as resumable; `scripts/ci/run_identity_codex_launcher_probes_ci.sh` now proves positive authoritative resume plus negative missing-session and invalid-session fail-close fixtures; and `README.md` plus the `v1.6.14` governance/review pair now explicitly freeze `resume <host-thread-uuid>` as the Codex recovery target distinct from launcher `--session-id run:<...>` tuple closure. | Keep launcher command discovery judged by fresh-shell executability rather than shortcut presence or host-thread UUID alone. `resume <UUID>` remains the recovery anchor for prior records, `--session-id run:<...>` remains launcher-side tuple closure only, and any future resume recommendation must fail-close unless both are authoritatively resolved and semantically non-collapsed. |
| ISSUE-034 `v1.6.14` launcher command discovery now canonicalizes preferred command surfaces under ambient catalog mismatch | CLOSED | `v1.6.14` | `scripts/render_identity_codex_launcher.py` now aligns `preferred_start_command` with `recommended_start_command` whenever `catalog_context_reason=ambient_catalog_mismatch_requires_explicit_catalog`, and aligns `preferred_resume_command` with the same canonical resume surface under mismatch instead of leaving stale short launchers on the preferred lane; short launchers now survive only as `shortcut_start_command`, `shortcut_resume_command`, and `copyable_commands.*.shortcut` reference fields; `scripts/ci/run_identity_codex_launcher_probes_ci.sh` now proves the mismatch lane by replaying an alternate catalog path, asserting canonicalized preferred start surfaces, and proving that stale short resume shortcuts are suppressed from the preferred lane when tuple closure is not authoritative under that mismatched catalog; and `README.md` plus the `v1.6.14` governance/review pair now freeze that short launchers may remain visible under mismatch only as convenience/reference surfaces, never as the preferred primary operator surface. | Keep `preferred_*` semantically identical to the canonical primary operator surface under catalog mismatch. Short launchers may remain visible for reference, but once the bundle knows explicit `--catalog` / `--session-id` are required, those convenience shortcuts must not outrank or dual-rank above the canonical fresh-shell command. |
| ISSUE-035 `v1.6.14` installed short launchers now stay pinned to their governed install catalog under ambient catalog drift | CLOSED | `v1.6.14` | `scripts/identity_codex_launcher_common.py` now renders per-identity shortcut shims with explicit `--catalog <resolved-catalog>` forwarding for both `commands` and `exec`, so ambient `IDENTITY_CATALOG` drift can no longer silently rebind `id-<identity-id>` to a foreign catalog; `scripts/install_identity_codex_launcher.py` and `scripts/render_identity_codex_launcher.py` now pass the governed catalog into shortcut rendering; `scripts/validate_identity_codex_launcher.py` now fail-closes stale installed shortcuts missing that catalog binding; `scripts/render_identity_codex_launcher.py` command-bundle/exec surfaces now also consume `validate_identity_runtime_mode_guard.py` in observational mode so repo-metadata fallback identities fail-close with machine-readable admissibility payloads instead of traceback-only crashes while launcher-owned env/catalog mismatch semantics remain intact; `scripts/ci/run_identity_codex_launcher_probes_ci.sh` now proves both command-bundle and dry-run success for the installed shortcut under injected env/catalog mismatch plus fresh-shell PATH exposure of `${CODEX_HOME}/bin` exactly once, and also proves structured fail-close for runtime-unadmitted repo-metadata identities; and `README.md` plus the `v1.6.14` governance/review pair now freeze that this execution-stability pinning does not relabel the mismatch discovery surface away from the explicit generic command. | Keep execution-time shortcut stability and operator-facing discovery semantics separate: the installed short launcher may internally pin itself to its governed catalog for fail-close execution, but the mismatch command bundle must still keep the explicit generic `--catalog` surface as the preferred/recommended operator-visible primary command. Runtime-admissibility truth stays shared-validator-owned, so repo-metadata fallback identities must fail-close through the same machine-readable admissibility split rather than leaking traceback-only launcher behavior. |
| ISSUE-036 `RQ-046` receipt-family bundle projection and release-freeze budget ceiling lagged final 1.6.x hardening | CLOSED | `v1.6.x release-freeze` | `scripts/identity_context_continuity_common.py` plus `scripts/validate_identity_context_continuity_receipts.py` now project inherited `RQ-046` requirement truth as bundle-compatible `required_contract` / `auto_required_signal` / `contract_key` / `contract_id` / `contract_derivation_mode` fields and fail-close non-canonical `receipt_family_contract_id` drift with `IP-ICREC-005`; `scripts/ci/run_identity_context_continuity_probes_ci.sh` now proves both the positive projection and the negative contract-drift fixture; direct live replay of `python3 scripts/required_gate_bundle_runner.py --catalog ../.identity/catalog.local.yaml --identity-id base-repo-audit-expert-v3 --operation scan --target-name identity_context_continuity_receipts --json-only` now returns `bundle_status=PASS_REQUIRED`; the final audited `1.6.x` control-plane release-freeze baseline is now re-anchored through the canonical sequenced materializer `python3 scripts/materialize_control_plane_surfaces.py --write --json-only`, which delegates budget refresh before status refresh and restores `validate_control_plane_budget.py` / `validate_control_plane_status_sync.py` to `PASS_REQUIRED` without hand-edited budget shortcuts or write-order races; and `docs/governance/identity-v1.6x-release-closure-governance.md` plus `docs/review/protocol-remediation-audit-ledger-v1.6x-release-closure.md` now freeze the version-boundary law that `1.6.x` closes current-universe debt before `1.7.x` begins future-facing work. | Keep target-probe-capable validators bundle-compatible even when requirement truth is inherited from upstream contracts rather than a standalone task key, keep control-plane budget/status closure machine-maintained through the canonical sequenced materializer after sanctioned 1.6.x hardening growth instead of manual envelope edits or compatibility loosening, and keep unresolved current-universe closure debt inside `1.6.x` rather than exporting it into `1.7.x`. |
| ISSUE-037 weak live linkage closure is now machine-landed across trio/prompt/sample/loop consumer lanes | CLOSED | `v1.6.19` | `ASB16-RQ-055` / `rq_055_identity_weak_live_linkage_differential_audit_contract_v1` remain the machine-law intake, and the final producer/backfill lane is now landed through `scripts/weak_live_current_run_projection_common.py`. `scripts/create_identity_pack.py` auto-wires the route optimization + roundtable contracts for new packs, `scripts/repair_contract_backfill.py` materializes governed current-run sample-family live reports plus route/feedback receipts for adopted packs, and `scripts/execute_identity_upgrade.py` now emits the same projection family on fresh runs. Direct runtime replay on both `base-repo-audit-expert-v3` and `custom-creative-ecom-analyst` now returns `overall_linkage_status=PASS_REQUIRED` and `operational_closure_class=full_operational_closure`; direct self-run on `base-repo-audit-expert-v3` produced `identity-upgrade-exec-base-repo-audit-expert-v3-1774398915.json` with `weak_live_current_run_projection_status=PASS_REQUIRED` and current-run projection refs embedded in `artifacts`. `bash scripts/ci/run_identity_weak_live_linkage_probes_ci.sh` remains green, and its hermetic closure proof is explicitly preserved as bounded probe evidence rather than substituted for real-runtime truth. | Keep the additive shared machine-law lane frozen: preserve `ASB16-RQ-055`, keep sample/self-test families available, keep semantic-center truth and live-bridge truth machine-separate, and reopen this line only on future machine regression rather than on historical sample/meta wording alone. |
| ISSUE-038 broadcast-delivery and identity communication transport convergence are now protocol-owned and fleet-closed | CLOSED | `v1.6.20` | `docs/governance/identity-broadcast-communication-convergence-governance-v1.6.20.md` and `docs/review/protocol-remediation-audit-ledger-v1.6.20-broadcast-communication-convergence.md` now freeze `rq_053_identity_broadcast_delivery_contract_v1` and `rq_054_identity_communication_transport_contract_v1`; `scripts/identity_broadcast_delivery_common.py`, `scripts/run_identity_broadcast_delivery.py`, `scripts/validate_identity_broadcast_delivery.py`, and `scripts/check_identity_broadcast_migration_closure.py` land the dedicated broadcast-delivery adoption lane; `scripts/identity_communication_transport_common.py`, `scripts/run_identity_communication_transport.py`, `scripts/validate_identity_communication_transport.py`, and `scripts/check_identity_communication_transport_closure.py` land the aggregate communication-transport convergence lane; `scripts/create_identity_pack.py`, `scripts/repair_contract_backfill.py`, `scripts/identity_creator.py`, `scripts/required_gate_bundle_runner.py`, `scripts/ci/run_required_runtime_gates_ci.sh`, and `scripts/release_readiness_check.py` now consume the pair; `bash scripts/ci/run_identity_broadcast_delivery_probes_ci.sh`, `bash scripts/ci/run_identity_communication_transport_probes_ci.sh`, and both workspace-runtime closure checkers now return `PASS_REQUIRED`; and direct runtime replay on `base-repo-audit-expert-v3` plus the other active workspace-local identities is green through the shared convergence executor without pack-local scripts or manual patching. | Keep `rq_053` and `rq_054` semantically separate: broadcast delivery remains the host-gateway broadcast adoption/projection lane, identity communication transport remains the aggregate convergence surface across handoff/collaboration/protocol-feedback/broadcast, the topic stays broader than strict identity-to-identity-only messaging, and future rollout must continue through shared backfill/creator/runner/closure-checker infrastructure instead of validator loosening, compatibility shortcuts, or pack-local repairs. |
| ISSUE-039 terminal clean truth / canonical publishability / negative-feedback veto are now protocol-owned machine law | CLOSED | `v1.6.21` | `docs/governance/identity-terminal-truth-cleanliness-governance-v1.6.21.md` and `docs/review/protocol-remediation-audit-ledger-v1.6.21-terminal-truth-cleanliness.md` now freeze `rq_056_identity_terminal_truth_cleanliness_contract_v1`; `scripts/terminal_truth_cleanliness_common.py`, `scripts/validate_terminal_truth_cleanliness.py`, and `bash scripts/ci/run_terminal_truth_cleanliness_probes_ci.sh` land the shared contract/validator/probe family, now including explicit terminal-state equivalence projection (`completed_clean`, `review_pending`, `revalidation_pending`, `repair_pending`, `retry_pending`, `quarantined`, `failed_terminal`, `non_terminal_pending`) plus fail-close adoption-mismatch detection and generic clean-completion alias-surface fail-close (`overall_status` / `final_status` / `status` / `result` / `outcome` / `done` / `completed`); `scripts/create_identity_pack.py`, `scripts/repair_contract_backfill.py`, and `scripts/execute_identity_upgrade.py` now emit the same projection family on new packs, adopted packs, and fresh execution reports; and `scripts/required_gate_bundle_runner.py`, `scripts/validate_required_contract_coverage.py`, and `scripts/ci/run_required_runtime_gates_ci.sh` now consume the new lane. Direct runtime replay on the current workspace-local `base-repo-audit-expert-v3` report now fail-closes as non-clean terminal truth because the report is still pre-mutation-gate blocked, while the same payload can still return `terminal_state_machine_status=PASS_REQUIRED` when that non-clean state is coherently projected rather than ambiguously narrated. | Keep execution closure truth, clean terminal truth, canonical publishability, explicit pending-state semantics, and generic completion alias surfaces machine-distinct. Review-required execution closure may remain legal at the lower layer, but governed dirty signals must veto clean terminal truth and canonical publishability, non-clean states must stay explicitly classified rather than flattened into an undifferentiated dirty bucket, and generic completed/done alias surfaces must not silently reintroduce clean-terminal claims after the higher-order lane has already failed them. Future work may extend verdict breadth, but it must not collapse review-required closure into “execution invalid”, weaken dirty-signal veto semantics, or permit dirty reports to masquerade as clean terminal truth. |


| ISSUE-040 Chat history is being misused as handoff state and lane closure lacks a durable skeleton | CLOSED | `c09a3a6`, `7dc829e32a4fc7a2a01757ed02aa15512aa790cb`, `908b8348d22c0583408cc6dfc4acd97217a03579` | Accepted closure chain freezes `no card, no handoff`, `no durable execution receipt, no continuation claim`, and `reopen is machine-triggered only`, so handoff, continuation, and reopen semantics are now machine-admitted through the adopted ISSUE-040 closure chain rather than chat-native recap. | Keep issue-register truth synced to the accepted ISSUE-040 closure chain only; do not reopen family semantics or re-admit chat-native handoff/takeover state outside the canonical machine-admitted lane surfaces. |
| ISSUE-041 Subagent/probe residue teardown is not part of closure and lifecycle governance is incomplete | CLOSED | `9fdb1114ed63a467846141a9049cc949f2b5e131`, `a929b0267f3c50a827b1385123f081f487806efd`, `3aed210` | Accepted closure chain freezes that `closure is incomplete when teardown receipts are missing`, `child tmp/probe/runtime residue without owner binding is not admitted`, `nested governed-root replay is not admitted`, and `guard cleanup deletes only machine-admitted stale residue and must not overreach live runtime`. | Keep issue-register truth synced to the accepted ISSUE-041 closure chain only; do not reopen lifecycle semantics or relax the owner-binding, replay-exclusion, or live-runtime guard boundaries. |
| ISSUE-042 Result-only execution accounting and fail-close closure gating are missing | CLOSED | `63fa59804fc2a2b49d44a1a96245e40ff02cf8e0`, `e20fe7f7ce028463bcfa0dafbee3d857bfb1d62f`, `0dfbdcf6b52ad9c1f3df762dca4a3af4814471af` | Accepted closure chain freezes `not written = not progressed`, `not validated = not complete`, and `not committed = not closed`, so narrative effort, broad confidence, or chat-only closure wording are not admitted as delivery without durable output, admitted validation, and bounded commit evidence. | Keep issue-register truth synced to the accepted ISSUE-042 closure chain only; do not reopen family semantics or re-admit narrative progress/completion/closure claims without machine evidence. |
| ISSUE-043 | CLOSED | non_owner_machine_law_reinforcement_admission_contract_v1 | cross-layer whole-lane reinforcement completion |machine_law_reinforcement_may_be_admitted_from_root_middle_or_consumer_surfaces_without_redefining_accepted_root_law accepted_root_law_ref required; whole_lane_completion_status admitted canonical_owner_truth_preservation_status preserved; root_semantic_redefinition_status not_redefined do not overwrite ISSUE-044/045 truth; silent whole-lane reopen fail-close|
| ISSUE-044 Identity instance protocol delta adoption state is not machine-visible and relevant protocol drift is not fail-closed | CLOSED | `ebfe53bfb5c124a0dd8bf50d13bbe8f1133a3acc` | `docs/governance/identity-instance-protocol-delta-adoption-governance-v1.6.x.md`, `docs/review/protocol-remediation-audit-ledger-v1.6.x-instance-protocol-delta-adoption.md`, `scripts/instance_protocol_delta_adoption_contract_common.py`, `scripts/validate_instance_protocol_delta_adoption.py`, and `scripts/ci/run_instance_protocol_delta_adoption_probes_ci.sh` now freeze `instance_protocol_delta_adoption_contract_v1` so `protocol_current_head`, `last_seen_protocol_commit`, `last_adopted_protocol_commit`, `capability_families`, `relevant_unadopted_commit_count`, `protocol_delta_adoption_status`, `protocol_delta_adoption_mode`, `state_path`, and `stale_reasons` are machine-visible; authority resolution, protocol owner-surface readiness, and instance-local adoption markers are checked together; and probe coverage fail-closes `relevant_protocol_delta_pending_adoption`, `protocol_owner_surface_not_ready`, `instance_local_adoption_markers_missing`, and `protocol_authority_resolution_failed` without collapsing `protocol_delta_adoption` into `instance_script_protocol_adoption`. | Keep authoritative protocol-head adoption state machine-visible and fail-close on relevant unadopted deltas; keep protocol-side readiness and instance-local adoption markers jointly required before advancing adopted head; and keep `protocol_delta_adoption` semantically distinct from `instance_script_protocol_adoption`. |
| ISSUE-045 Lane segmented infrastructure admission is not frozen and handoff still depends on chat reconstruction | CLOSED | `v1.6.x` root infra (segmented lane closure) | Canonical governance/review owner surfaces now freeze segmented lane entry and closure as one admitted law family: `root`, `middle`, and `tail` each have bounded entry rules, continuation must consume repo-visible baton fields (`lane_id`, `governing_law`, `fixed_write_set`, `layer_state`, `next_exact_action`, `validation_bundle`, `reopen_triggers`, `commit_gate`), execution-loop state is machine-visible through `planning_budget_status`, `scope_lock_status`, `mutation_phase_entry_status`, `repeated_plan_restatement_status`, `repeated_reanchor_status`, `repeated_compaction_without_progress_status`, `execution_loop_status`, and `stale_reasons`, and chat recap is not an admitted baton surface. | Tail truth-sync may synchronize accepted closure only; it must not reinterpret, replace, or originate accepted root law, it must not become a source of root law, and locked-scope repetition without mutation progress fail-closes as `execution_loop_not_entering_mutation_phase`. |
| ISSUE-046 Scope-locked mutation-phase runtime enforcement is implemented but not canonically landed as a formal workbook/register issue | CLOSED | `scope_locked_mutation_phase_runtime_enforcement` formal promotion | Accepted owner surfaces already enforce the scope-locked mutation-phase runtime guard; ISSUE-046 canonically lands that accepted runtime enforcement into workbook/register as the formal tracking issue without rewriting the owner-surface law body or reopening the accepted ISSUE-044 adoption bridge. | Keep workbook/register truth synced to the accepted owner-surface runtime guard only; allow formal promotion, truth-sync, ordering, and parseable status normalization, preserve ISSUE-043 / ISSUE-044 accepted closure truth, and keep ISSUE-045 / ISSUE-047 / ISSUE-048 and other blockers out of scope. |
| ISSUE-047 Post-mutation closeout invariant was accepted on owner surfaces but lacked a canonical workbook/register issue landing | CLOSED | `1e0ce227daf87d2f7853c4d7ff8c964bdfe499b0` | Accepted phase-1 root-infra closure already froze `execution_loop_after_mutation_not_closing` on canonical governance/review/common/validator/probe surfaces; ISSUE-047 now lands that accepted truth as the sole formal workbook/register issue so long-term audit and review can anchor to one machine-admitted phase-1 closure without reopening the lane. | Keep workbook/register truth synced to the accepted phase-1 anchor only; do not reinterpret or replace the accepted owner-surface law, and do not absorb ISSUE-043/044/045/046, `protocol_lane_headstamp_continuity`, or other blockers into this formal issue landing. |
| ISSUE-048 Formal issue truth-sync / probe companion sync / exact staged-path gate boundary redefinition is canonically landed for release closure | CLOSED | `1e0ce227daf87d2f7853c4d7ff8c964bdfe499b0` | Accepted phase-1 root-infra closure remains frozen at `1e0ce227daf87d2f7853c4d7ff8c964bdfe499b0`; ISSUE-048 independently lands workbook/register truth-sync, release-closure horizon sync, and real probe companion sync so release audit and probe expectations converge on one canonical `ISSUE-048` universe without reopening the accepted owner-surface law. | Keep formal issue truth synced to the accepted phase-1 anchor only; synchronize release-closure horizon/probe companion expectations without changing governing law or fail-close semantics, and do not absorb ISSUE-043/044/045/046, `protocol_lane_headstamp_continuity`, or other blockers into this lane. |

## 4) Frozen interpretation

1. `CLOSED` here means the active machine-gated owner lane has accepted the stop condition and no governed current row remains live-red on this workbook family.
2. Future architecture expansion, release hygiene, or workspace cleanliness must not silently rewrite a `CLOSED` row back to open debt without a governed machine regression.
3. If a future scan finds a new fact, classify it first:
   - reopen the existing issue only when the same governed invariant regresses;
   - otherwise route it into a new issue or a later architecture lane.
