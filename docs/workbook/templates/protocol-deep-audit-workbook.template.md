# Identity Protocol __WORKBOOK_MINOR__ Deep Audit Workbook

Status: Scaffolded workbook family template
Layer: protocol
Scope: serial deep-audit intake for the `__WORKBOOK_MINOR__` workbook family, radiating over the active `__WORKBOOK_PATCH_LANE__` stream lanes
Authority boundary: this workbook becomes canonical intake/routing surface only when `__WORKBOOK_REGISTRY_CURRENT_REF__` selects `__FAMILY_REGISTRY_DOC__`; it never overrides `__ISSUE_REGISTER_DOC__` for current-status authority.
Scaffold boundary: generated by `__SCAFFOLD_SCRIPT__`; activation requires the explicit pointer switch frozen by `__GOVERNANCE_DOC__`.

## 0) Fixed role boundary

1. This scaffold exists to prepare the intake/routing surface for a future workbook family inside `identity-protocol-local`.
2. Before activation it is a governed scaffold, not an active audit SSOT.
3. Stream semantics remain owned by governance/review docs under `docs/governance/` and `docs/review/`.
4. Optional projection stubs for this family remain mirror-only until activation/backfill.

## 1) Current control-plane alias refs

- `identity/protocol/mappings/workbook-registry.current.yaml`
- `identity/protocol/mappings/stream-doc-registry.current.yaml`
- `identity/protocol/mappings/control-plane-status.current.yaml`

## 2) Activation boundary

1. The scaffolded family registry is `__FAMILY_REGISTRY_DOC__`.
2. `__ISSUE_REGISTER_DOC__` remains the only current-status authority for this family after activation.
3. Projection stubs must be refreshed and promoted separately from scaffold generation.

## 3) Root-cause clusters (initial scaffold)

### RC-01 Pending intake cluster

Symptoms:

- Populate when the first cross-stream issue for `__WORKBOOK_MINOR__` is opened.

Root cause:

- Populate when intake evidence exists for this family.
