# Identity Protocol __WORKBOOK_MINOR__ Issue Register

Status: Scaffolded workbook family template
Layer: protocol
Scope: cross-stream issue governance for the `__WORKBOOK_MINOR__` workbook family, radiating over the active `__WORKBOOK_PATCH_LANE__` stream lanes
Authority: this file becomes the authoritative current-status table only when `__WORKBOOK_REGISTRY_CURRENT_REF__` selects `__FAMILY_REGISTRY_DOC__`.
Scaffold boundary: generated by `__SCAFFOLD_SCRIPT__`; activation requires the explicit pointer switch frozen by `__GOVERNANCE_DOC__`.

## 0) Fixed role boundary

1. This scaffold does not replace stream-owner review ledgers under `docs/review/`.
2. This scaffold becomes current-status authority only after explicit workbook-family activation.
3. Until activation, any projection exported from this family remains projection-only and non-authoritative.
4. `__DEEP_AUDIT_DOC__` is the companion routing workbook for this family once activated.

## 1) Current control-plane alias refs

- `identity/protocol/mappings/workbook-registry.current.yaml`
- `identity/protocol/mappings/stream-doc-registry.current.yaml`
- `identity/protocol/mappings/control-plane-status.current.yaml`

## 2) Activation boundary

1. The scaffolded family registry is `__FAMILY_REGISTRY_DOC__`.
2. The current pointer remains unchanged until an explicit activation step updates `__WORKBOOK_REGISTRY_CURRENT_REF__`.
3. Projection stubs for this family must not be treated as live status mirrors before activation/backfill.

## 3) Issue table

| Issue | Status | Primary owner lane | Current closure anchor | Freeze rule |
| --- | --- | --- | --- | --- |
