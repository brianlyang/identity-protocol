# Identity Protocol v1.6.x Release Closure Summary — 2026-03-23

## Purpose

This document freezes the v1.6.x release boundary for the three core protocol roles:

- base-repo architect
- audit expert
- closure expert

It exists to prevent three kinds of drift during the v1.6.x to v1.7.x handoff:

1. reopening already-closed shared protocol streams
2. misclassifying instance evidence debt as shared protocol incompleteness
3. carrying v1.6.x unresolved-state ambiguity into v1.7.x planning

## Unified release verdict

- Shared protocol infrastructure for the frozen v1.6.14 / v1.6.16 / v1.6.17 / v1.6.18 streams: **Go for v1.6.x closure**
- Current working-tree promotion state: **not yet fully green**
- Base-repo-audit-expert-v3 instance readiness: **not fully green**
- v1.7.x planning handoff: **Go**

The precise interpretation is:

- the shared protocol owner streams for v1.6.14, v1.6.16, v1.6.17, and v1.6.18 are closed strongly enough to freeze for release
- the current working tree still carries one separate promotion blocker on the control-plane budget surface, so release-cut promotion must not claim full green until that blocker is either closed or excluded from the release branch
- the remaining red on the base-repo-audit-expert-v3 instance is evidence debt in discovery/tool/vendor families, not a reopened shared protocol defect

## Closed shared protocol streams

### v1.6.14 — launcher owner stream

The launcher command surface is now closed as shared infrastructure:

- short command surface works for the instance owner stream
- canonical fresh-shell command surface remains available
- UUID-based resume remains authoritative
- command discovery distinguishes preferred reference surface from canonical fresh-shell surface
- continuity support remains internal launcher support, not a new user-facing command family

The v1.6.14 stream is therefore not blocked by short-command ambiguity anymore.

### v1.6.16 — continuity and receipt-family stream

The context continuity stack is now closed as shared infrastructure:

- continuity artifact validation is machine-consumed
- reentry brief validation is machine-consumed
- reentry consumption validation is machine-consumed
- receipt-family validation is machine-consumed
- continuity probe suite passes with both positive and negative-path coverage

The v1.6.16 stream is therefore not blocked by continuity semantics drift anymore.

### v1.6.17 — routing-learning-strengthening stream

The strengthened third/fourth loop and 4→1 bridge are now closed as shared infrastructure:

- route discovery convergence is machine-consumed
- feedback operational prompt strengthening is machine-consumed
- feedback-to-judgement loopback is machine-consumed
- first-loop revalidation remains authoritative
- conflict demotion, rollback, and negative writeback remain fail-close
- live roundtrip proof is machine-visible and passes

The v1.6.17 stream is therefore not blocked by loopback ambiguity or roundtrip-proof incompleteness anymore.

### v1.6.18 — artifact-family-routing stream

The memory-like artifact interpretation surface is now closed as shared infrastructure:

- generic `memory` wording is no longer a canonical protocol sink
- the eight protocol-owned artifact families remain frozen by semantic owner + canonical root + producer + consumer
- raw dialogue retention remains the first family-specific machine landing
- whole-matrix routing remains machine-consumed through `rq_052`
- optional families can stay `SKIPPED_NOT_REQUIRED` without manufacturing fake red
- quarantine-only `runtime/memory-absorption/**` remains non-promotable on active success paths
- canonical reference visual atlas governance now covers the breakthrough-sequence, loop, and artifact-family atlas families through registry-driven SSOT rows plus control-plane visibility

The v1.6.18 stream is therefore not blocked by generic-memory ambiguity or missing routing closure anymore.

## Additional shared-infrastructure hardening completed during closure

The final v1.6.x stabilization work also closed several cross-stream infrastructure gaps:

1. stale execution report refreshability is now bounded to one explicit strict-update family rather than treated as open-ended drift
2. collaboration-trigger self-test assets are now generated from a protocol-owned canonical sample primitive
3. blocker taxonomy canonicalization is now enforced through shared backfill primitives rather than instance-local edits
4. blocker-surface backfill is now applicability-scoped, so non-applicable probe packs do not fail-close incorrectly
5. native-chat hard-guard prompt injection is now idempotent and no longer causes newline-churn drift across repeated repair passes
6. instance pack topology recognizes the new collaboration sample families as governed runtime structure
7. current-round required-contract coverage now reuses canonical run-bound orchestration on the release / three-plane / e2e / CI owner surfaces instead of evaluating pre-bundle or unbound coverage calls
8. required gate drift auditing now fail-closes missing `--run-id` / `--report-selected-path` coverage parity and coverage-before-bundle ordering regressions on the guarded orchestrator surfaces
9. reference visual atlas governance is now registry-driven at the control-plane surface, so newly landed atlas families must declare canonical doc / asset root / validator / status key instead of freehand drift

These are infrastructure upgrades, not business patches.

## Current working-tree promotion blocker outside the frozen streams

This deep-audit round also confirmed one **separate** blocker that must stay semantically distinct from the frozen v1.6.x owner streams:

- `control_plane_budget_status = FAIL_REQUIRED`
- `control_plane_status = FAIL_REQUIRED`
- `control_plane_status_sync_status = PASS_REQUIRED` after the status snapshot was truth-synced to the current machine result

The blocker reason is:

- the current working tree contains an in-flight workbook-projection control-plane addition
- that addition introduces the `IP-WFPR-*` error-code family through `scripts/render_active_workbook_projections.py`
- the current no-rebound budget is exceeded by exactly `+3` raw error codes and `+1` error-code family, which matches that new workbook-projection surface

This must be interpreted carefully:

- it does **not** reopen the frozen launcher / continuity / routing-learning-loopback / artifact-family-routing streams
- it does **not** negate the current-round required-contract coverage orchestration closure
- it **does** mean the current working tree is not yet promotion-ready as a release cut until the workbook-projection stream is either closed with proper budget/governance adoption or kept out of the v1.6.x release branch

## What remains open, and why it does not reopen shared closure

The base-repo-audit-expert-v3 instance still lacks three evidence families:

- tool installation evidence
- vendor API discovery evidence
- vendor API solution evidence

These deficits currently surface through the corresponding validators for tool-installation and vendor-solution coverage.

This remaining red must be interpreted as:

- **instance evidence debt**
- **not** reopened shared protocol semantics
- **not** justification to weaken fail-close contracts
- **not** justification to relabel v1.6.14, v1.6.16, or v1.6.17 as unfinished

The separate control-plane budget blocker above must also be interpreted correctly:

- **working-tree promotion debt**
- **not** proof that the frozen v1.6.14 / v1.6.16 / v1.6.17 / v1.6.18 semantics were falsely closed
- **not** justification to silently raise budgets without governance
- **not** justification to hide current status drift by leaving the control-plane status file stale

## Release boundary rules

### What is frozen for v1.6.x

The following boundaries should no longer be reopened during v1.6.x closure:

- launcher owner-stream semantics from v1.6.14
- continuity/reentry/receipt-family semantics from v1.6.16
- loop-3 / loop-4 / bounded 4→1 loopback semantics from v1.6.17
- artifact-family routing / memory-like interpretation semantics from v1.6.18
- shared canonicalization and backfill semantics that now govern blocker, collaboration-sample, and prompt-hard-guard repairs

### What must stay separated

The release conversation must keep these planes separated:

1. shared protocol closure
2. instance-specific evidence readiness
3. v1.7.x carry-forward work

No remaining instance debt should be rewritten as proof that v1.6.x shared protocol closure is incomplete.

## Role-specific handoff

### For the base-repo architect

- treat the shared semantics of v1.6.14, v1.6.16, v1.6.17, and v1.6.18 as frozen
- do not reopen those streams to compensate for instance-local evidence debt
- carry only genuinely new infrastructure work into v1.7.x

### For the audit expert

- use the launcher, continuity, loopback, control-plane, and docs validators as the authoritative proof surface
- include artifact-family routing and reference-atlas governance validators in that proof surface
- classify remaining red by ownership layer before escalating it
- reject any attempt to merge instance evidence gaps back into shared closure state

### For the closure expert

- close shared v1.6.x closure items on the basis of the frozen streams above
- track the remaining discovery/tool/vendor evidence gaps as instance-owned debt
- preserve the release boundary when drafting the v1.7.x carry-forward list

## v1.7.x carry-forward rule

v1.7.x should start from this frozen assumption:

- shared v1.6.x infrastructure is closed for launcher ownership, continuity, routing-learning loopback, and artifact-family routing
- remaining work should be either:
  - instance evidence completion, or
  - genuinely new shared infrastructure that emerges from repeated multi-identity evidence

v1.7.x should not begin by relitigating whether the v1.6.x shared closure was real.

## Final summary

The correct final v1.6.x closure statement is:

The shared protocol foundation is ready to freeze and release. The remaining red on the audited instance is real, but it is scoped to instance-owned discovery and vendor evidence rather than shared protocol incompleteness. Therefore v1.6.x may close on the shared protocol plane while those residual evidence debts are either completed locally or carried forward explicitly into v1.7.x planning.
