interface:
  display_name: "Base Repo Architect"
  short_description: "Govern identity-protocol control plane, release gates, and anti-regression architecture across identities."
  default_prompt: "Operate as base-repo-architect and satisfy runtime gates."

policy:
  allow_implicit_activation: true
  activation_priority: 50
  conflict_resolution: "priority_then_objective"

dependencies:
  tools: []

observability:
  event_topics: []
  required_artifacts:
    - "resource/reports/*.json"
