schema_version: 1
completeness_family: protocol_root_machine_registry_completeness
completeness_version: v1
registry_directory_rel_path: identity/protocol/mappings
registry_current_file: identity/protocol/mappings/root-corpus-registry.current.yaml
validator_script: scripts/validate_protocol_root_machine_registry_completeness.py
probe_script: scripts/ci/run_protocol_root_machine_registry_completeness_probes_ci.sh
common_script: scripts/root_machine_registry_completeness_common.py
status_key: protocol_root_machine_registry_completeness_status
error_codes:
- IP-RMRC-001
- IP-RMRC-002
- IP-RMRC-003
root_family_prefix: root-
current_suffix: .current.yaml
version_regex: ^root-[a-z0-9-]+\.v[0-9]+\.yaml$
require_current_version_pairs: true
require_self_describing_families: true
repo_rel_path_scope_policy: repo_root_relative_only
repo_rel_path_escape_policy: fail_closed
repo_rel_path_role_typing_policy: root_protocol_surface_patterns_required
required_descriptor_fields:
- validator_script
- probe_script
- common_script
- status_key
- error_codes
required_descriptor_field_modes:
  validator_script: repo_rel_path
  probe_script: repo_rel_path
  common_script: repo_rel_path
  status_key: validator_status_key
  error_codes: validator_error_code_list
required_repo_rel_path_patterns:
  validator_script: ^scripts/validate_protocol_root_[a-z0-9_]+\.py$
  probe_script: ^scripts/ci/run_protocol_root_[a-z0-9_]+_probes_ci\.sh$
  common_script: ^scripts/root_[a-z0-9_]+_common\.py$
anchor_checks:
- rel_path: identity/protocol/IDENTITY_PROTOCOL_DESIGN_PHILOSOPHY.md
  required_markers:
  - '### Machine-registry completeness must stay explicit'
  - Machine-registry law does not become canonical merely because a mapping file
  - A governed root mapping family must be admitted into the registry directory
  - registry-completeness failure rather than a
  - Admission without discoverable enforcement still leaves the machine
  - A lawful root mapping family must therefore disclose the validator, probe,
  - shared-common, emitted status-key, and emitted error-code surfaces that govern
    it.
  - Those repo-relative path surfaces must remain repo-root relative and
  - Absolute-path capture or parent-escape capture would let local filesystem
    accident impersonate governed protocol law.
  - descriptor paths that bypass repo-root-relative discipline.
  - Repo-relative descriptor surfaces must also remain role-typed.
  - role-swapped descriptor paths inside repo root.
- rel_path: identity/protocol/README.md
  required_markers:
  - '## Root machine-registry completeness discipline'
  - a law-bearing root mapping family does not gain canonical status from on-disk
    presence alone;
  - a governed root mapping family must appear in the admitted machine-registry child
    set, normally as a current file plus its active versioned file;
  - if a root mapping family exists on disk but is absent from that admitted child
    set, registry completeness has failed and current-turn consumption must fail-close.
  - 4. an admitted root mapping family must disclose its validator, probe, shared-common,
    emitted status-key, and emitted error-code enforcement surfaces to the machine
    world;
  - Hidden enforcement knowledge does not satisfy registry completeness.
  - Repo-relative descriptor surfaces must also stay repo-root relative and
  - if they exist locally.
  - Repo-relative descriptor surfaces must also remain role-typed; validator,
    probe, and shared-common paths are not interchangeable repo files.
- rel_path: identity/protocol/IDENTITY_PROTOCOL.md
  required_markers:
  - '## Root machine-registry completeness boundary'
  - Law-bearing root mapping families under `identity/protocol/mappings/` become canonical
    only when admitted by the machine-registry directory child set.
  - On-disk presence without registry admission does not authorize current-turn consumption,
    legal ingress, or bundle membership.
  - Registry-completeness drift is a root-law failure, not a convenience-layer warning.
  - 5. Registry admission without discoverable validator/probe/common/status-key/error-code
    surfaces is
    still incomplete.
  - 6. Repo-relative descriptor surfaces disclosed by an admitted family must remain
    repo-root relative and repo-contained; absolute-path or parent-escape capture
    is non-compliant.
  - 7. Repo-relative descriptor surfaces disclosed by an admitted family must also
    stay role-typed; validator, probe, and shared-common path classes are not
    interchangeable.
- rel_path: identity/protocol/IDENTITY_RUNTIME.md
  required_markers:
  - '## Runtime registry-completeness boundary'
  - Runtime may consume only root mapping families admitted by governed machine-registry
    completeness law.
  - A root mapping file present on disk but absent from the admitted child set is
    non-canonical for runtime legality.
  - Runtime must fail-close on registry-completeness drift rather than loading the
    most convenient on-disk mapping.
  - 5. Runtime must discover validator/probe/common/status-key/error-code surfaces
    from admitted mapping-family descriptors rather than hidden local knowledge.
  - 6. Runtime must reject descriptor paths that are absolute or escape repo root;
    repo-relative descriptor surfaces must stay repo-root relative and repo-contained.
  - 7. Runtime must also reject role-swapped descriptor paths; validator, probe,
    and shared-common surfaces must stay role-typed rather than merely repo-local.
